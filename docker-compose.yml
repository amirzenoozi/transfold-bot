version: '3.8'

services:
  transfold-bot:
    build: .
    container_name: transfold_telegram_bot
    restart: always
    environment:
      - TELEGRAM_BOT_TOKEN=BOT_TOKEN_HERE
      - ADMIN_ID=ADMIN_ID_HERE
    volumes:
      - ./database:/app/database
